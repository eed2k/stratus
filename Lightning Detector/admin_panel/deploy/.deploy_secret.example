# Optional: paste your NEW Vultr root password here (one line, no quotes).
# Get it from Vultr -> Server -> Settings -> Reset Root Password.
# Leave empty if you use the SSH key method (CONSOLE_ADD_KEY.sh).
